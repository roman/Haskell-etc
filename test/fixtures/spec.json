{

    "etc/filepaths": ["test/fixtures/one.json",
                      "test/fixtures/two.json"],

    "etc/entries": {

        "user": {
            "etc/spec": {
                "env": "USER",
                "default": "roman"
            }
        },

        "host": {
            "etc/spec": {
                "env": "TEST_HOST",
                "default": "zootopia"
            }
        },

        "password": {
            "etc/spec": {
                "env": "NON_EXISTING_PASSWORD"
            }
        }

    }

}
