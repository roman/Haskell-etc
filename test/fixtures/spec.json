{
    "user": {
        "config/spec": {
            "default": "roman",
            "env": "USER",
            "optparse": {
                "long": "user",
                "short": "u",
                "metavar": "USER",
                "help": "User of the system",
                "type": "string",
                "required": false
            }
        }
    },

    "password": {
        "config/spec": {
            "env": "NON_EXISTING_PASSWORD",
            "default": "abc123"
        }
    }
}
