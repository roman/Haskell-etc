name: etc-cli
author: open-source@roman-gonzalez.info

dependencies:
  - aeson
  - aeson-better-errors
  - base
  - prettyprinter
  - rio
  - optparse-applicative
  - etc

library:
  source-dirs: src
  exposed-modules:
  # Public API
  - Etc.Cli
  # Internal API
  - Etc.Internal.Cli.Plain.Parser
  - Etc.Internal.Cli.Plain.Resolver
  - Etc.Internal.Cli.Error
  - Etc.Internal.Cli.Types

tests:
  etc-cli-tests:
    main: TestSuite.hs
    source-dirs: test
    dependencies:
      - raw-strings-qq
      - aeson-qq
      - base
      - hspec
      - QuickCheck
      - rio
      - etc-cli
